services:
  db-mysql:
    image: mysql:5.7.40
    restart: always
    environment:
      - MYSQL_DATABASE= ${DB_NAME}
      - MYSQL_ROOT_PASSWORD= ${DB_PASSWORD}
    ports:
      - "3390:${MYSQL_DB_PORT}"
